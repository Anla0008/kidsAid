---

---

<div class="groen_boks">
  <img class="image" src="./gallery/groenbillede.svg" alt="mand" />
  <img class="greenblob" src="./gallery/greenblob.svg" alt="Gul blob" />
  <div class="tekst_indhold">
    <div class="text-wrapper">“</div>

    <p>
      “Som ansvarlige for donationer i [Virksomhed] er det mig en ære at kunne sige, at vi har været med til at bidrage i den gode sags tjeneste. Da jeg hørte om KidsAid Tryllerne var jeg ikke et sekund i tvivl om, at det var et koncept vores virksomhed skulle støtte op om. Børnene på hospitalerne fortjene hver og en al den støtte og omsorg de overhovedet kan få”<br /><br />
      — Donationsansvarlig, [Virksomhed]
    </p>
  </div>
</div>

<style>
  /**** Mobil *****/
  @media (max-width: 768px) {
    .groen_boks {
      position: relative;
      width: 90%;
      max-width: 600px;
      margin: 0 auto;
      text-align: center;
      margin-top: 100px;
    }

    /* Image of the person */
    .image {
      max-width: 100%;
      height: auto;
      z-index: -1;
    }

    /* Yellow blob */
    .tekst_indhold {
      position: relative; /* Juster i forhold til dens normale placering */
      transform: translateY(-50px) translateX(-30px); /* Flytter teksten op og til venstre */
      text-align: left; /* Holder teksten venstrejusteret */
      max-width: 65ch; /* Begrænser bredden for bedre læsbarhed */
      margin-left: 70px; /* Tilføjer afstand fra venstre */
      font-size: 13px; /* Tilpas tekststørrelse */
      z-index: 1; /* Sørger for, at teksten er foran blobben */
    }

    .text-wrapper {
      font-size: 3rem; /* Stor skriftstørrelse for overskriften */
      font-weight: bold; /* Gør teksten fed */
      color: #333; /* Mørkegrå farve */
      width: fit-content; /* Gør bredden kun så stor som indholdet */
      margin: 0 auto; /* Centrerer teksten horisontalt */
    }

    .greenblob {
      position: absolute; /* Gør blobben absolut placeret */
      bottom: -20px; /* Flytter blobben længere ned */
      left: 47.5%; /* Centrerer blobben horisontalt */
      transform: translateX(-50%) scaleX(1.1); /* Centrerer og strækker blobben lidt */
      max-width: 110%; /* Lader blobben fylde mere end sin container */
      height: auto; /* Bevarer proportionerne */
      z-index: 0; /* Sørger for, at blobben er bag teksten */
    }
  }

  /****** Desktop ******/
  @media (min-width: 769px) {
    .groen_boks {
      position: relative;
      display: flex;
      justify-content: flex-start;
      align-items: center;
      width: 90%;
      max-width: 1200px;
      margin: 50px auto;
      gap: 0.5rem;
      text-align: left;
      margin-top: 200px;
      margin-bottom: 100px;
    }

    /* Image of the person */
    .image {
      flex: 0 0 40%;
      max-width: 100%;
      height: auto;
      margin-right: 0;
    }

    /* Yellow blob */
    .greenblob {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-30%, -55%);
      max-width: 75%;
      height: auto;
      z-index: -1;
    }

    /* Text content */
    .tekst_indhold {
      flex: 0 0 50%;
      max-width: 38ch;
      margin-left: 20px;
      padding-left: 0;
      font-size: 20px;
      margin-top: 70px;
    }

    .text-wrapper {
      font-size: 5rem;
      font-weight: bold;
      color: #333;
      margin: auto;
      width: fit-content;
      margin-top: -10rem;
      transform: translate(-200%);
    }
  }
</style>
